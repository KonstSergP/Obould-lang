module Math
import Out;

fn factorial(n: i64) -> i64
var result: i64;
{
    if n <= 1 {
        result = 1;
    } else {
        result = n * factorial(n - 1);
    }
    return result;
}

fn init() -> void {
    Out.Int(factorial(6));
    Out.Ln();
}
